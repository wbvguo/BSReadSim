# Product data

`examples/` contains small, versioned inputs used by the documentation. These
files are included in the source distribution but are not installed in the
wheel.

Large human-maintained datasets belong in ignored `dev/data/`. Agent-created
or downloaded task data belong in the relevant dated `workspace/` directory.
